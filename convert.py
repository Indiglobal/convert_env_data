import csv
import argparse

parser = argparse.ArgumentParses()
parser.addArgument('-s', '--source', default='data.csv')
parser.addArgument('-d', '--destination', default='clean_data.csv')

if __name__ = '__main__':
    with open(source, 'r', encoding='utf_16_le'):
        
